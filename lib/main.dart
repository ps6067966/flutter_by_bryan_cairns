import 'package:flutter/material.dart';

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Flutter Demo',
      theme: ThemeData(
        primarySwatch: Colors.blue,
      ),
      // home: CustomisedElevatedButton(),
      // home: CustomizedElevatedButtonWithParameters(),
      // home: CustomizedTextButton(),
      // home: CustomizedIconButton(),
      // home: CustomizedTextField(),
      // home: CustomizedCheckBox(),
      // home: CustomizedRadioButton(),
      // home: CustomizedSwitch(),
      // home: CustomizedSlider(),
      // home: CustomizedDatePicker(),
      
    );
  }
}
